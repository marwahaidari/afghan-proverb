<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>
    <%= post.translationEn %> - Proverb Details
  </title>

  <!-- Font Awesome CDN for icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap');

    body {
      font-family: 'Montserrat', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #d0e6ff 0%, #eef7ff 100%);
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
      padding: 20px;
    }

    .card {
      background: white;
      max-width: 600px;
      width: 100%;
      border-radius: 16px;
      box-shadow: 0 12px 30px rgba(37, 99, 235, 0.2);
      padding: 35px 45px;
      box-sizing: border-box;
      text-align: center;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .card:hover {
      transform: translateY(-8px);
      box-shadow: 0 20px 40px rgba(37, 99, 235, 0.35);
    }

    h1 {
      font-size: 2rem;
      color: #1e3a8a;
      margin-bottom: 30px;
      font-weight: 700;
      letter-spacing: 1px;
    }

    .field {
      margin: 18px 0;
      font-size: 1.15rem;
      color: #2c3e50;
      text-align: left;
      display: flex;
      align-items: center;
      gap: 12px;
      line-height: 1.4;
    }

    .field strong {
      color: #2563eb;
      min-width: 100px;
      font-weight: 700;
      font-size: 1.1rem;
    }

    .field i {
      color: #3b82f6;
      min-width: 24px;
      font-size: 1.3rem;
    }

    .actions {
      margin-top: 35px;
      display: flex;
      justify-content: center;
      gap: 30px;
    }

    .actions a,
    .actions button {
      background: #2563eb;
      color: white;
      padding: 12px 22px;
      border-radius: 10px;
      text-decoration: none;
      font-weight: 700;
      border: none;
      cursor: pointer;
      display: flex;
      align-items: center;
      gap: 10px;
      box-shadow: 0 5px 15px rgba(37, 99, 235, 0.4);
      transition: background-color 0.25s ease, box-shadow 0.25s ease;
      font-size: 1.1rem;
    }

    .actions a:hover,
    .actions button:hover {
      background: #1e40af;
      box-shadow: 0 8px 22px rgba(30, 64, 175, 0.6);
    }

    button {
      font-size: 1rem;
    }

    /* Responsive tweak */
    @media (max-width: 650px) {
      .card {
        padding: 25px 20px;
      }

      h1 {
        font-size: 1.6rem;
        margin-bottom: 20px;
      }

      .field strong {
        min-width: 85px;
        font-size: 1rem;
      }

      .field i {
        font-size: 1.1rem;
        min-width: 20px;
      }

      .actions {
        flex-direction: column;
        gap: 15px;
      }

      .actions a,
      .actions button {
        justify-content: center;
        width: 100%;
      }
    }
  </style>

</head>

<body>

  <div class="card">
    <h1><i class="fas fa-feather-alt"></i>
      <%= post.translationEn %>
    </h1>

    <div class="field"><i class="fas fa-language"></i> <strong>Dari:</strong> <span>
        <%= post.textDari %>
      </span></div>
    <div class="field"><i class="fas fa-language"></i> <strong>Pashto:</strong> <span>
        <%= post.textPashto %>
      </span></div>
    <div class="field"><i class="fas fa-info-circle"></i> <strong>Meaning:</strong> <span>
        <%= post.meaning %>
      </span></div>
    <div class="field"><i class="fas fa-tag"></i> <strong>Category:</strong> <span>
        <%= post.category %>
      </span></div>

    <div class="actions">
      <a href="/post/<%= post.id %>/edit" title="Edit Post">
        <i class="fas fa-edit"></i> Edit
      </a>

      <form action="/post/<%= post.id %>/delete" method="POST"
        onsubmit="return confirm('Are you sure you want to delete this post?');" style="display:inline;">
        <button type="submit" title="Delete Post">
          <i class="fas fa-trash-alt"></i> Delete
        </button>
      </form>

      <a href="/" title="Back to All Posts">
        <i class="fas fa-arrow-left"></i> Back
      </a>
    </div>
  </div>

</body>

</html>